import plants0 from "/javascripts/families/gesneriaceae.js";
const plantlist0 = plants0;
console.log(plantlist0);

let genusCollection = [];
let limit = 500;
const doLoop = () => {
  for (let i = 0; i < limit; i++) {
    // import plants from `/javascripts/families/plants${i}`;
  } //seems imports can't be looped? need to be assigned first i guess
};
